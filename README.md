# Random-Quran-Page-Generator
A modern React (Vite) web application that dynamically fetches and displays a random Quran page using the Al-Quran Cloud API. Each refresh or click generates a new random page with formatted ayahs.
